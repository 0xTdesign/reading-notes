```

// comparison Operators

a == b // loose equal to
a === b // strict equal to 
a != b // not equal to 
a !== b // Stict not equal to
a > b // greater than
a < b // less than
a >= b // greater than equal to
a <= b // less than or equal to


// logical operators

a && b // is a AND b true
a || b // is a OR b true
!a // NOT - inverts the boolean value of a


a = 'tim'
!a would be False 

boolean = True or False 

Evaluating Comparisons 

( Operand - Oprator - Operand 

( 1 === 2 ) 


// Type coercion 

'10' > 1 
True

"5" == 5
true


// initial value, condition, increment 

for ( let i = 0; i < 10; i++)


arry.length
let arr = ["a" , 2, false,{}, "I am last"]
console.log(arr[i])

let arr = ["a" , 2, false, {}, "i am last"];

for (let i = 0 i < arr.length: i++){
console.log(arr[i])

```

```
let answers = ["a","b","c"];
let guess;

let attempts = 2;

// loop through until the attempts run out
while(attempts > 0) {

  // at the start of each loop, make them guess
  guess = prompt("Guess the answer");

  // loop though the correct answers
  for(let i = 0; i < answers.length; i++) {
    // check if the guess is one of the answer
    if(guess === answers[i]) {
      //if it is, tell them, remove attempts and stop the for loop
      alert("Thats right!");
      attempts = 0;
      hasWon = true;
      break;
    }
  }

  // take one attempt away each time
  attempts--;
}

```



